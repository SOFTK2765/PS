#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long a, b, s;
    long long tmp;
    tmp = a+b;
    if(s<tmp)
    {
        printf("NO");
        return 0;
    }
    tmp = s%tmp;
    while

    return 0;
}