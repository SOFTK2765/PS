#include <bits/stdc++.h>

using namespace std;

int main()
{
    long long n;
    scanf("%lld", &n);
    printf("%lld", (long long)sqrt(n)!=sqrt(n)?(long long)(sqrt(n)+1):(long long)sqrt(n));

    return 0;
}