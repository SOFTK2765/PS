#include <bits/stdc++.h>

using namespace std;

int a[5000001];

int main()
{
    int n, l;
    scanf("%d %d", &n, &l);
    for(int i=0;i<n;i++)
        scanf(" %d", &a[i]);
    set<pair<int, int>> bst;
    for(int i=0;i<n;i++)
    {
        
    }

    return 0;
}