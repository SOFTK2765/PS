#include <bits/stdc++.h>

using namespace std;

char a[600001];

typedef struct Node* nodePoint;
typedef struct Node
{
    int n;
    nodePoint left = 0, right = 0;
};

int main()
{
    scanf("%s", a);
    int l = strlen(a);
    nodePoint start;
    for(int i=0;i<l;i++)
    {

    }

    return 0;
}