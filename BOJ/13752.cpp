#include <bits/stdc++.h>

using namespace std;

int main()
{
    int t;
    scanf("%d", &t);
    while(t--)
    {
        int tmp;
        scanf(" %d", &tmp);
        while(tmp--) printf("=");
        printf("\n");
    }

    return 0;
}