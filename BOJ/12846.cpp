#include <bits/stdc++.h>

using namespace std;

int a[100001];

int main()
{
    int n;
    scanf("%d", &n);
    for(int i=0;i<n;i++)
        scanf(" %d", &a[i]);
    int posa = 0, posb = 0;
    vector<pair<int, int>> v;
    v.push_back({a[0], 0}); 

    return 0;
}