#include <bits/stdc++.h>

using namespace std;

int main()
{
	int n;
	scanf("%d", &n);
	for(int i=0;i<n;i++)
		scanf(" %d", &a[i].first)
	for(int i=0;i<n;i++)
		scanf(" %d", &a[i].second);

	return 0;
}