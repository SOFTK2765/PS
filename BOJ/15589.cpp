#include <bits/stdc++.h>

using namespace std;

pair<int, int> a[100001];

int main()
{
    int n;
    scanf("%d", &n);
    vector<int> v;
    for(int i=0;i<n;i++)
    {
        scanf(" %d %d", &a[i].first, &a[i].second);
        v.push_back(tmp1);
        v.push_back(tmp2);
    }
    sort(v.begin(), v.end());
    int size = v.size();
    for(int i=0;i<2*n;i++)
    {
        mp
    }


    return 0;
}