#include <bits/stdc++.h>

using namespace std;

char tmp[51];

vector<pair<string, int>> v;

int main()
{
    int n;
    scanf("%d", &n);
    for(int i=0;i<n;i++)
    {
        memset(tmp, 0, sizeof(tmp));
        scanf(" %s", tmp);
        for(vector::iterator)
    }

    return 0;
}