#include <bits/stdc++.h>

using namespace std;

char a[2501];
bool check[2501];

int main()
{
    scanf("%s", a);
    int l = strlen(a);
    if(l%2==1)
    {
        for(int i=0;i<l/2;i++)
        {
            memset(check, 0, sizeof(check))
            for(int j=l/2+i;j<l;j++)
            {
                rf(j);
            }

            for(int j=l/2-i;j>=0;j--)
            if(!check)
            f(i)
        }
    }


    return 0;
}